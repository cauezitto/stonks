import { Story, Meta } from '@storybook/react'
import PaddingWrapper from '.'

export default {
  title: 'PaddingWrapper',
  component: PaddingWrapper
} as Meta

export const Default: Story = () => <PaddingWrapper />
